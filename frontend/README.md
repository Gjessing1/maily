# @maily/frontend

Placeholder workspace. The React + Vite + Tailwind PWA (with `vite-plugin-pwa`, Dexie/IndexedDB
caching, Service Worker + Web Push) is built in **Phase 3** — see [docs/ROADMAP.md](../docs/ROADMAP.md).
