<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!--
          Background notifications. Maily asks its own server what has arrived, on an
          alarm it re-arms after every check (MailyPushAlarm) — no foreground service, and
          therefore none of the permanent "app is running" notice Android demands of one.

          BOOT_COMPLETED and MY_PACKAGE_REPLACED both clear the armed alarm: a reboot
          cancels every alarm on the device, and an in-place update is not followed by a
          boot broadcast. Inert unless notifications were enabled on this device.
        -->
        <receiver
            android:name=".MailyPushReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ gates notifications behind a runtime grant (MailyNativePlugin asks). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Re-arm the mail check after a reboot, which cancels every alarm the app had set. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
      Hold the CPU awake for the few seconds the check takes. AlarmManager's own wake lock
      lasts only as long as onReceive, and the work continues past it (MailyPushReceiver),
      so an idle phone would otherwise suspend mid-request.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
      Deliberately absent: FOREGROUND_SERVICE(_SPECIAL_USE), because nothing runs
      indefinitely any more, and REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, because the poll
      uses setAndAllowWhileIdle and therefore fires through Doze without an exemption.
      No exact-alarm permission either: mail has no appointed time, so the inexact alarm
      is the honest declaration as well as the cheaper one.
    -->
</manifest>
