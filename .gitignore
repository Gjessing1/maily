# --- Personal planning docs (kept local, not committed) ---
CLAUDE.md
.claude/
docs/

# --- Dependencies ---
node_modules/
.npm/

# --- Build output ---
dist/
dev-dist/
build/
*.tsbuildinfo
# Built PWA copied here by backend/Dockerfile for production serving.
backend/public/

# --- Env & secrets ---
.env
.env.*
!.env.example
# Firebase client config for the APK's FCM channel. Not a credential, but it carries the
# project's identifiers and this repo mirrors to a public GitHub remote. Keep a copy with
# the keystore -- an APK built without it simply has notifications disabled.
android/app/google-services.json

# --- Persistent data (host: /home/gjessing/data/maily) ---
data/
attachments/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# --- Logs / misc ---
*.log
npm-debug.log*
.DS_Store
coverage/
.vite/
test-results/
playwright-report/
